import { queryOptions } from '@tanstack/react-query'
import { get{{pascalCase name}} } from '.'

export function {{camelCase name}}QueryOptions() {
  return queryOptions({
    queryKey: ['{{kebabCase name}}'],
    queryFn: get{{pascalCase name}},
  })
}
