import { queryOptions } from '@tanstack/react-query'
import { get{{pascalCase name}}, type TGetParams{{pascalCase name}} } from '.'

export function {{camelCase name}}QueryOptions(token?: string) {
  return function (params: TGetParams{{pascalCase name}}) {
    return queryOptions({
      queryKey: ['{{kebabCase name}}', params],
      queryFn: get{{pascalCase name}}(token)(params),
    })
  }
}
